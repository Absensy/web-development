# Гранит Памяти - Сайт ритуальных услуг

Современный веб-сайт для компании "Гранит Памяти", специализирующейся на изготовлении памятников и ритуальных услуг.

[![GitHub](https://img.shields.io/badge/GitHub-Repository-blue?logo=github)](https://github.com/Absensy/web-development)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

## 🚀 Технологии

- **Next.js 15** - React фреймворк
- **TypeScript** - типизация
- **Material-UI** - UI компоненты
- **Prisma** - ORM для работы с БД
- **SQLite** - локальная база данных
- **Локальное хранилище** - файлы хранятся локально в `public/uploads`
- **JWT** - аутентификация

## 📋 Функциональность

### Публичная часть
- Главная страница с информацией о компании
- Каталог памятников с фильтрацией
- Примеры работ
- Контактная информация
- Адаптивный дизайн

### Админ-панель
- Управление товарами и категориями
- Загрузка изображений
- Управление контентными блоками
- Управление примерами работ
- Безопасная JWT аутентификация

## 🛠️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/Absensy/web-development.git
cd web-development
```

### 2. Установка зависимостей
⚠️ **Важно**: Из-за конфликтов зависимостей используйте флаг `--legacy-peer-deps`:

```bash
npm install --legacy-peer-deps
```

### 3. Настройка переменных окружения
Создайте файл `.env.local` в корне проекта:

```env
# Database (SQLite - локальная БД)
DATABASE_URL="file:./dev.db"

# Cloud Storage Configuration (по умолчанию local)
CLOUD_STORAGE_PROVIDER=local  # local, cloudinary

# Cloudinary (опционально, если нужно использовать облачное хранилище)
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random
```

**Примечание**: Проект настроен для локального хостинга. Все данные хранятся локально:
- База данных: SQLite файл `prisma/dev.db`
- Файлы: папка `public/uploads/`

### 4. Настройка базы данных
```bash
# Генерация Prisma клиента
npm run db:generate

# Применение схемы к БД (создаст SQLite файл)
npm run db:push

# Заполнение тестовыми данными
npm run db:seed

# Экспорт данных в JSON (для статического экспорта)
npm run db:export
```

### 5. Запуск проекта
```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 🔐 Доступ к админ-панели

- **URL**: `http://localhost:3000/admin`
- **Пароль**: `Oleshuki2012`

## 📁 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── admin/             # Админ-панель
│   ├── api/               # API routes
│   └── catalog/           # Каталог товаров
├── components/            # React компоненты
├── hooks/                 # Кастомные хуки
├── lib/                   # Утилиты и конфигурация
├── contexts/              # React контексты
└── types/                 # TypeScript типы
```

## 🚀 Деплой

### GitHub Pages (Рекомендуется)

Проект настроен для автоматического деплоя на GitHub Pages:

1. **Настройка репозитория**:
   - Перейдите в Settings → Pages
   - Source: выберите "GitHub Actions"
   - Сохраните настройки

2. **Автоматический деплой**:
   - При каждом пуше в ветку `main` автоматически запускается сборка и деплой
   - GitHub Actions выполнит:
     - Экспорт данных из БД в JSON файлы (если доступна БД)
     - Статическую сборку Next.js
     - Публикацию на GitHub Pages
   - Сайт будет доступен по адресу: `https://[username].github.io/[repository-name]`

3. **Ручной экспорт данных** (перед первым деплоем):
   ```bash
   npm run db:export
   ```
   Это создаст JSON файлы в `public/data/` для статического использования.

4. **Локальная проверка статической сборки**:
   ```bash
   npm run export
   ```
   Это создаст папку `out/` со статическими файлами, которые можно проверить локально.

**Важно**: 
- SQLite база данных (`*.db` файлы) не коммитятся в Git (добавлены в `.gitignore`)
- Файлы из `public/uploads/` также не коммитятся
- Для статического деплоя используются экспортированные JSON файлы из `public/data/`
- API routes не работают в статическом режиме - все данные загружаются из JSON файлов

### Локальная разработка

Для локальной разработки с полным функционалом (включая API routes):

```bash
npm run dev
```

### Vercel / Netlify

1. Подключите репозиторий
2. Настройте переменные окружения
3. Деплой происходит автоматически

## 📚 Дополнительная документация

- [Настройка базы данных](./SETUP_DATABASE.md)
- [Настройка Cloudinary](./CLOUDINARY_SETUP.md)
- [Деплой в продакшен](./PRODUCTION_DEPLOYMENT.md)
- [Настройка Prisma](./PRISMA_SETUP.md)

## 🤝 Разработка

### Полезные команды
```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Запуск продакшен сборки
npm start

# Линтинг
npm run lint

# Проверка типов
npm run type-check
```

### Структура коммитов
- `feat:` - новая функциональность
- `fix:` - исправление багов
- `docs:` - обновление документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов

## 🔗 Ссылки

- **GitHub репозиторий**: [https://github.com/Absensy/web-development](https://github.com/Absensy/web-development)
- **GitHub Pages** (если настроено): [https://absensy.github.io/web-development](https://absensy.github.io/web-development)
- **Issues**: [https://github.com/Absensy/web-development/issues](https://github.com/Absensy/web-development/issues)

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](./LICENSE) для деталей.
